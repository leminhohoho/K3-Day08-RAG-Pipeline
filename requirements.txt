# Core
python-dotenv>=1.0.0
requests>=2.31.0
# LangChain 0.2.x (required by ragas 0.1.21) requires NumPy <2 on Python 3.12.
numpy>=1.26.0,<2.0.0

# Task 2 - Crawling
# Crawl4AI has its own fast-moving OpenAI/LiteLLM dependency chain. Install it
# in an isolated environment from requirements-crawl.txt so it cannot force
# this legacy RAGAS/LangChain runtime to backtrack or build LiteLLM from source.

# Task 3 - Document conversion
markitdown[pdf]>=0.1.0
fpdf2>=2.7.0

# Task 4 - Chunking & Indexing
# Keep this on the LangChain 0.2.x line: ragas==0.1.21 requires
# langchain-core<0.3, while text-splitters 0.3+ requires core>=0.3.
langchain-text-splitters==0.2.4
sentence-transformers>=2.2.0
chromadb>=0.5.0

# Task 6 - BM25 + TF-IDF
rank-bm25>=0.2.2
scikit-learn>=1.3.0

# Task 8 - PageIndex
pageindex>=0.1.0

# Task 10 - Generation
# langchain-openai==0.1.25 requires openai>=1.40,<2.
openai>=1.40.0,<2.0.0

# Group project - UI
streamlit>=1.35.0

# Group project - Evaluation
# Compatibility set for the legacy RAGAS API used by eval_pipeline.py.
# Do not upgrade one LangChain package independently: these versions share
# the same langchain-core>=0.2.43,<0.3 dependency range.
ragas==0.1.21
datasets>=2.14.0,<3.0.0
langchain==0.2.17
langchain-core==0.2.43
langchain-community==0.2.19
langchain-openai==0.1.25

# Testing (dung de cham diem bai ca nhan)
pytest>=7.0.0
